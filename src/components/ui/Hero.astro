<section class="hero">
  <div class="container">
    <div>
      <div class="hero-badge-group">
        <span class="hero-badge"><i class="fas fa-check-circle"></i> ২৪/৭ সক্রিয়</span>
        <span class="hero-badge"><i class="fas fa-shield-alt"></i> যাচাইকৃত</span>
        <span class="hero-badge"><i class="fas fa-map-pin"></i> আপনার এলাকা</span>
      </div>
      <h1>আপনার শহরের <span>সব সেবা</span> এক ঠিকানায়</h1>
      <p>আবাসন থেকে অ্যাম্বুলেন্স, টিউশন থেকে টেকনিশিয়ান — হাইপার কমিউনিটি প্ল্যাটফর্ম 'সহজি' এখন হাতের মুঠোয়।</p>
      <div style="display:flex; gap:16px; flex-wrap:wrap;">
        <a href="/services/job" class="btn-primary"><i class="fas fa-magnifying-glass"></i> সেবা খুঁজুন</a>
        <a href="/about" class="btn-outline"><i class="fas fa-play-circle"></i> কীভাবে কাজ করে</a>
      </div>
    </div>
    <div class="hero-image">
      <i class="fas fa-city"></i>
    </div>
  </div>
</section>

<style>
  .hero { padding: 60px 0 80px; background: linear-gradient(135deg, #F4F7FC 0%, #E9F0F9 100%); position: relative; overflow: hidden; }
  .hero .container { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
  .hero h1 { font-size: 3.4rem; font-weight: 700; line-height: 1.15; letter-spacing: -1px; }
  .hero h1 span { color: #FF6B35; position: relative; }
  .hero h1 span::after { content: ''; position: absolute; left: 0; bottom: 4px; width: 100%; height: 12px; background: rgba(255,107,53,0.2); border-radius: 20px; z-index: -1; }
  .hero p { font-size: 1.2rem; color: #4A5B6E; margin: 24px 0 36px; max-width: 500px; }
  .hero-badge-group { display: flex; gap: 16px; flex-wrap: wrap; margin-bottom: 16px; }
  .hero-badge { background: rgba(255,255,255,0.7); backdrop-filter: blur(4px); padding: 8px 18px; border-radius: 50px; font-size: 0.85rem; font-weight: 500; border: 1px solid rgba(255,107,53,0.15); }
  .hero-badge i { color: #FF6B35; margin-right: 8px; }
  .hero-image { background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 300" fill="%23FF6B35" opacity="0.08"><circle cx="100" cy="100" r="80"/><circle cx="300" cy="80" r="60"/><circle cx="220" cy="220" r="90"/><circle cx="50" cy="240" r="40"/></svg>') no-repeat center; background-size: cover; min-height: 300px; border-radius: 40px; background-color: #E2EAF5; display: flex; align-items: center; justify-content: center; font-size: 4rem; color: #1B2A4A; box-shadow: 0 30px 60px rgba(27,42,74,0.08); }
  @media (max-width: 768px) {
    .hero .container { grid-template-columns: 1fr; text-align: center; }
    .hero h1 { font-size: 2.4rem; }
    .hero p { margin-left: auto; margin-right: auto; }
    .hero-badge-group { justify-content: center; }
  }
</style>