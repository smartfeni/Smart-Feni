<section class="search-section">
  <div class="container">
    <div class="search-card">
      <i class="fas fa-search"></i>
      <input type="text" placeholder="কী সার্ভিস দরকার? (যেমন: প্লাম্বার, ডাক্তার, টিউশন)" />
      <button class="btn-primary"><i class="fas fa-arrow-right"></i> খুঁজুন</button>
    </div>
  </div>
</section>

<style>
  .search-section { margin-top: -40px; position: relative; z-index: 10; }
  .search-card {
    background: #fff;
    padding: 20px 28px;
    border-radius: 80px;
    box-shadow: 0 20px 50px rgba(27,42,74,0.08);
    display: flex;
    gap: 12px;
    align-items: center;
    flex-wrap: wrap;
    justify-content: center;
  }
  .search-card i { color: #FF6B35; font-size: 1.2rem; }
  .search-card input {
    flex: 1;
    min-width: 200px;
    border: none;
    padding: 14px 8px;
    font-size: 1rem;
    outline: none;
    background: transparent;
  }
  .search-card input::placeholder { color: #A0B3C9; }
  .search-card .btn-primary { padding: 12px 36px; }
  @media (max-width: 768px) {
    .search-card { border-radius: 40px; flex-direction: column; }
  }
</style>