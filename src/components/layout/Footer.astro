---
// ============================================================
// কম্পোনেন্ট: ফুটার
// ব্র্যান্ড: স্মার্ট ফেনী
// ফিচার: Brand (top, full-width) + ৩টা সমান কলাম (সেবা/কোম্পানি/যোগাযোগ)
// ============================================================

const contact = {
  email: 'info@smartfeni.com',
  phone: '+8801816355833',
};
---

<footer>
  <div class="container">
    <!-- ব্র্যান্ড সেকশন (ফুল উইথ, উপরে) -->
    <div class="footer-brand">
      <div class="social-links">
        <a href="https://www.facebook.com/smartfeni247" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
      </div>
      <a href="/" class="wordmark">SMART FENI</a>
      <p class="footer-tagline">"প্রয়োজন আপনার, দায়িত্ব আমাদের"</p>
      <p class="footer-desc">Your digital gateway to all essential services in Feni city.</p>
    </div>

    <!-- ৩টা সমান কলাম -->
    <div class="footer-top">
      <div class="footer-col">
        <h4>জনপ্রিয় সেবা</h4>
        <a href="/services/courier">ডেলিভারি হিরো</a>
        <a href="/services/online-shop">অনলাইন শপ</a>
        <a href="/services/car-rental">গাড়ি ভাড়া</a>
        <a href="/services/ride">রাইড হিরো</a>
      </div>

      <div class="footer-col">
        <h4>কোম্পানি</h4>
        <a href="/about">আমাদের সম্পর্কে</a>
        <a href="/privacy-policy">প্রাইভেসি পলিসি</a>
      </div>

      <div class="footer-col">
        <h4>যোগাযোগ</h4>
        <a href={`mailto:${contact.email}`}>
          <i class="fas fa-envelope"></i> {contact.email}
        </a>
        <a href={`tel:${contact.phone}`}>
          <i class="fas fa-phone"></i> {contact.phone}
        </a>
        <button class="install-btn" id="installBtn" type="button">
          <i class="fas fa-mobile-screen-button"></i> অ্যাপ ইনস্টল করুন
        </button>
      </div>
    </div>

    <div class="footer-bottom">
      <p>© ২০২৬ স্মার্ট ফেনী। সর্বস্বত্ব সংরক্ষিত। &nbsp;•&nbsp; PROUDLY MADE FOR FENI.</p>
    </div>
  </div>
</footer>

<!-- iOS টিপ (আগের মতো) -->
<div class="ios-tip-overlay" id="iosTipOverlay">
  <div class="ios-tip-box">
    <button class="ios-tip-close" id="iosTipClose" aria-label="বন্ধ করুন"><i class="fas fa-xmark"></i></button>
    <i class="fas fa-mobile-screen-button ios-tip-icon"></i>
    <h4>আইফোনে অ্যাপ ইনস্টল করুন</h4>
    <ol>
      <li>নিচে <i class="fas fa-arrow-up-from-bracket"></i> শেয়ার বাটনে ট্যাপ করুন</li>
      <li>"Add to Home Screen" সিলেক্ট করুন</li>
      <li>উপরে ডানে "Add" ট্যাপ করুন</li>
    </ol>
  </div>
</div>

<style is:global>
  footer {
    position: relative;
    background-color: #14171C;
    background-image:
      radial-gradient(circle at 88% 0%, rgba(255, 107, 53, 0.14) 0%, transparent 42%),
      radial-gradient(circle at 6% 100%, rgba(255, 107, 53, 0.05) 0%, transparent 45%),
      radial-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px);
    background-size: auto, auto, 18px 18px;
    color: #9AA5B1;
    padding: 26px 0 14px;
    border-radius: 24px 24px 0 0;
    margin-top: 32px;
    overflow: hidden;
  }

  footer .container {
    position: relative;
    z-index: 1;
  }

  /* ===== ব্র্যান্ড সেকশন ===== */
  .footer-brand {
    text-align: center;
    padding-bottom: 16px;
    margin-bottom: 16px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
  }

  .social-links {
    display: flex;
    justify-content: center;
    gap: 8px;
    margin-bottom: 10px;
  }

  .social-links a {
    width: 30px;
    height: 30px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #9AA5B1;
    text-decoration: none;
    font-size: 0.8rem;
    transition: all 0.2s ease;
  }

  .social-links a:hover {
    background: #FF6B35;
    border-color: #FF6B35;
    color: #fff;
  }

  .wordmark {
    display: inline-block;
    font-size: 1.3rem;
    font-weight: 800;
    color: #fff;
    text-decoration: none;
    letter-spacing: -0.02em;
    margin-bottom: 6px;
  }

  .wordmark::after {
    content: '';
    display: block;
    width: 30px;
    height: 3px;
    background: #FF6B35;
    border-radius: 2px;
    margin: 5px auto 0;
  }

  .footer-tagline {
    font-family: Georgia, 'Times New Roman', serif;
    font-style: italic;
    font-size: 0.78rem;
    color: #FFB088;
    margin-bottom: 4px;
  }

  .footer-desc {
    font-size: 0.78rem;
    line-height: 1.4;
    opacity: 0.65;
    max-width: 320px;
    margin: 0 auto;
  }

  /* ===== ৩টা সমান কলাম ===== */
  .footer-top {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 16px;
    padding-bottom: 14px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    text-align: center;
  }

  .footer-col h4 {
    color: #fff;
    font-size: 0.82rem;
    margin-bottom: 8px;
    font-weight: 700;
  }

  .footer-col a {
    display: block;
    color: #9AA5B1;
    text-decoration: none;
    font-size: 0.75rem;
    padding: 3px 0;
    margin-bottom: 0;
    opacity: 0.85;
    transition: opacity 0.2s ease;
  }

  .footer-col a:hover {
    opacity: 1;
    color: #FFB088;
  }

  .footer-col a i {
    width: 14px;
    color: #FF6B35;
    margin-right: 4px;
  }

  .install-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    background: rgba(255, 107, 53, 0.1);
    border: 1px solid rgba(255, 107, 53, 0.4);
    color: #FF9A6E;
    padding: 7px 14px;
    border-radius: 30px;
    font-weight: 600;
    font-size: 0.72rem;
    cursor: pointer;
    margin-top: 10px;
    transition: all 0.2s ease;
    font-family: inherit;
  }

  .install-btn:hover {
    background: #FF6B35;
    color: #fff;
    box-shadow: 0 0 14px 1px rgba(255, 107, 53, 0.45);
  }

  .install-btn.hidden {
    display: none;
  }

  .footer-bottom {
    display: flex;
    justify-content: center;
    align-items: center;
    padding-top: 10px;
    font-size: 0.68rem;
    opacity: 0.55;
    text-align: center;
  }

  .footer-bottom p {
    margin: 0;
  }

  /* ============================================================
     মোবাইল রেসপন্সিভ
     ============================================================ */
  @media (max-width: 768px) {
    .footer-brand {
      padding-bottom: 12px;
      margin-bottom: 12px;
    }

    .footer-brand .wordmark {
      font-size: 1.15rem;
    }

    .footer-tagline {
      font-size: 0.72rem;
    }

    .footer-desc {
      font-size: 0.72rem;
      max-width: 100%;
    }

    .social-links a {
      width: 27px;
      height: 27px;
      font-size: 0.75rem;
    }

    .footer-top {
      grid-template-columns: 1fr 1fr 1fr;
      gap: 8px;
    }

    .footer-col h4 {
      font-size: 0.68rem;
      margin-bottom: 6px;
    }

    .footer-col a {
      font-size: 0.62rem;
      padding: 3px 0;
      line-height: 1.35;
    }

    .footer-col a i {
      margin-right: 2px;
    }

    .install-btn {
      margin-top: 8px;
      padding: 5px 8px;
      font-size: 0.6rem;
      width: 100%;
      max-width: 130px;
    }

    .footer-bottom {
      font-size: 0.6rem;
      padding-top: 10px;
    }
  }

  /* ===== ছোট মোবাইলের জন্য ===== */
  @media (max-width: 480px) {
    footer {
      padding: 20px 0 12px;
      border-radius: 18px 18px 0 0;
      margin-top: 24px;
    }

    .footer-top {
      gap: 6px;
    }

    .footer-brand .wordmark {
      font-size: 1.05rem;
    }

    .footer-col h4 {
      font-size: 0.62rem;
    }

    .footer-col a {
      font-size: 0.58rem;
    }

    .install-btn {
      font-size: 0.56rem;
      padding: 4px 6px;
    }

    .footer-bottom {
      font-size: 0.56rem;
    }
  }

  /* ===== iOS টিপ স্টাইল ===== */
  .ios-tip-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(15, 23, 42, 0.6);
    z-index: 2100;
    align-items: flex-end;
    justify-content: center;
    padding: 0;
  }

  .ios-tip-overlay.open {
    display: flex;
  }

  .ios-tip-box {
    position: relative;
    background: #fff;
    width: 100%;
    max-width: 420px;
    border-radius: 24px 24px 0 0;
    padding: 32px 28px 36px;
    text-align: center;
    animation: iosTipUp 0.25s ease;
  }

  @keyframes iosTipUp {
    from { transform: translateY(100%); }
    to { transform: translateY(0); }
  }

  .ios-tip-close {
    position: absolute;
    top: 16px;
    right: 16px;
    background: #F4F7FC;
    border: none;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    color: #5A6E85;
    cursor: pointer;
  }

  .ios-tip-icon {
    font-size: 2rem;
    color: #FF6B35;
    margin-bottom: 12px;
  }

  .ios-tip-box h4 {
    color: #1B2A4A;
    font-size: 1.1rem;
    margin-bottom: 18px;
  }

  .ios-tip-box ol {
    text-align: right;
    padding-right: 26px;
    color: #5A6E85;
    font-size: 0.88rem;
    line-height: 2;
  }

  .ios-tip-box ol i {
    color: #FF6B35;
    margin: 0 4px;
  }
</style>

<script>
  const installBtn = document.getElementById('installBtn');
  const iosOverlay = document.getElementById('iosTipOverlay');
  const iosClose = document.getElementById('iosTipClose');

  const isIOS = /iphone|ipad|ipod/i.test(navigator.userAgent);
  const isStandalone = window.matchMedia('(display-mode: standalone)').matches
    || window.navigator.standalone === true;

  if (isStandalone) {
    installBtn?.classList.add('hidden');
  }

  installBtn?.addEventListener('click', async () => {
    if (isIOS) {
      iosOverlay?.classList.add('open');
      return;
    }

    const promptEvent = window.deferredInstallPrompt;
    if (!promptEvent) {
      alert('আপনার ব্রাউজারে ইতিমধ্যে ইনস্টল করা আছে অথবা এই মুহূর্তে ইনস্টল অপশন উপলব্ধ নেই।');
      return;
    }

    promptEvent.prompt();
    await promptEvent.userChoice;
    window.deferredInstallPrompt = null;
  });

  iosClose?.addEventListener('click', () => iosOverlay?.classList.remove('open'));
  iosOverlay?.addEventListener('click', (e) => {
    if (e.target === iosOverlay) iosOverlay.classList.remove('open');
  });

  window.addEventListener('smartfeni:install-done', () => {
    installBtn?.classList.add('hidden');
  });
</script>